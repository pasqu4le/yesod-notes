$(function() {
  $("##{rawJS newNoteFormId}").submit(function(event) {
    event.preventDefault();

    var title = $("##{rawJS newNoteFormId}").find("input.input").val();
    var content = $("##{rawJS newNoteFormId}").find("textarea").val();
    // (Browsers that enforce the "required" attribute on the textarea won't see this alert)
    if (!content) {
      alert("Please fill out your new note content first.");
      return;
    }

    // Make an AJAX request to the server to create a new note
    $.ajax({
      url: '@{AddNoteR}',
      type: 'POST',
      contentType: "application/json",
      data: JSON.stringify({
        title: title,
        content: content,
      }),
      success: function (data) {
        console.log(data);
        $("##{rawJS noteListId}").prepend(data);
      },
      error: function (data) {
        console.log("Error creating note: " + data);
      },
    });

  });
});
