<div .box>
    <p .title>Add a new Note
    <form .form method=post action=@{AddNoteR} enctype=#{formEnctype}>
        ^{formWidget}
        <hr>
        <div .buttons.is-right>
            <button .button.is-primary type="submit">
                <span .icon>
                    <i .fas.fa-plus>
                <span>Add note